#!/bin/zsh
# Start Claude Code with DeepSeek provider - fully isolated
PROVIDER_DIR="$HOME/.config/zsh/bin/ai/providers/deepseek"
export ANTHROPIC_AUTH_TOKEN="$DEEPSEEK_API_KEY"
exec claude --dangerously-skip-permissions --settings "$PROVIDER_DIR/settings.json" "$@"
