#!/bin/zsh
# Start Claude Code with MiniMax provider - fully isolated
PROVIDER_DIR="$HOME/.config/zsh/bin/ai/providers/minimax"
export ANTHROPIC_AUTH_TOKEN="$MINIMAX_API_KEY"
exec claude --dangerously-skip-permissions --settings "$PROVIDER_DIR/settings.json" "$@"
