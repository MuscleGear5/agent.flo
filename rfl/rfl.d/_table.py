"""Shared Python table renderer — sourced inline by rfl handlers via: python3 _table.py | ..."""
# This file is imported via: exec(open('.../_table.py').read()) — but we inline it for speed.
# Kept here as reference. Actual code is inlined in handlers.zsh.
